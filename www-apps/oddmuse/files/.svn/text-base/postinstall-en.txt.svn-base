Now go to http://${VHOST_HOSTNAME}/${VHOST_CGIBIN}/wiki.pl

You can edit that script to set \$DataDir

Supplied optional modules are installed in /usr/share/doc/${PF}/modules.
You can activate them by copying a subdirectory called modules in \$DataDir.
Note that some of these may require additional perl dependencies!

See http://www.oddmuse.org/cgi-bin/wiki/Web_Server_Setup on hints for
webservers setup.
